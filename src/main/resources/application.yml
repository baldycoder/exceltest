server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/excel?useSSL=false&serverTimezone=UTC&characterEncoding=UTF8
    username: root
    password: maonuojin2017
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true